# Data Integrity Validation Guide

Data integrity runs are read-only and never auto-fix data.

Checks cover missing profiles, workspace admins, broken brand/product/project links, broken creative request and generated version links, invalid share links, credit ledger mismatch placeholder, and pricing plans without feature policies.
